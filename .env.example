APP_ENV=development
PUBLIC_APP_URL=http://localhost:3000

# Add secrets only to an ignored .env.local file or your deployment secret store.
# Never commit API keys, database credentials, payment secrets, or signing keys.
